{% extends 'dashboard/front.html.twig' %}

{% block title %}Rendezvous{% endblock %}

{% block body %}
    <h1>Rendezvous</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rendezvou.id }}</td>
            </tr>
            <tr>
                <th>Nompatient</th>
                <td>{{ rendezvou.nompatient }}</td>
            </tr>
            <tr>
                <th>Nommedecin</th>
                <td>{{ rendezvou.nommedecin }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ rendezvou.date ? rendezvou.date|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Heure</th>
                <td>{{ rendezvou.heure ? rendezvou.heure|date('H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Etat</th>
                <td>{{ rendezvou.etat ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_rendezvous_back_index') }}">back to list</a>

    <a href="{{ path('app_rendezvous_back_edit', {'id': rendezvou.id}) }}">edit</a>

    {{ include('rendezvous_back/_delete_form.html.twig') }}
{% endblock %}
