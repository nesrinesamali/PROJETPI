{% extends 'base.html.twig' %}

{% block body %}

  <div class="container">
    <div class="row justify-content-center">
      <div class="col-md-8">
        <div class="card">
          <div class="card-header">
            <h1 class="text-center">Ajouter calendrier</h1>
          </div>
          <div class="card-body">
            {{ include('calendrier/form.html.twig', {'button_label': 'Add'}) }}
            <hr>
            
            <a href="{{ path('calendrier_index') }}" class="btn btn-secondary mt-3"><i class="fas fa-arrow-left"></i> Back to list</a>
          </div>
        </div>
      </div>
    </div>
  </div>
{% endblock %}