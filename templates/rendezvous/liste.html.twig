<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title> Liste des rendez-vous </title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f5f5f5;
            margin: 0;
            padding: 20px;
        }

        h1 {
            color: #333;
            text-align: center;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }

        th, td {
            border: 1px solid #ddd;
            padding: 12px;
            text-align: left;
        }

        th {
            background-color: #f2f2f2;
            font-weight: bold;
            color: red; /* Set th text color to red */
        }

        tbody tr:hover {
            background-color: #f9f9f9;
        }

        .category-column {
            width: 20%;
        }

        .price-column {
            width: 15%;
        }

        .description-column {
            width: 35%;
        }

        .name-column {
            width: 20%;
        }

        .currency {
            font-weight: bold;
        }
    </style>
</head>
<body>
<h1> Liste des rendez-vous </h1>
<table>
    <thead>
    <tr>
        <th>ID</th>
        <th >Nom Patient</th>
        <th >Nom Medecin</th>
        <th>Date</th>
        <th>Heure</th>
    </tr>
    </thead>
    <tbody>
    {% for rendezvou in rendezvous %}
        <tr>
            <td>{{ rendezvou.id }}</td>
            <td>{{ rendezvou.nompatient}}</td>
            <td>{{ rendezvou.nommedecin}}</td>
              <td>{{ rendezvou.date|date('m/d/Y') }}</td>
             <td>{{ rendezvou.heure|date('H:i:s') }}</td>
        </tr>
    {% endfor %}
    </tbody>
</table>
</body>
</html>